📩 SMS Spam Classifier

This is a **simple Machine Learning project** that can detect whether a message is **Spam** or **Not Spam (Ham)**.  
It’s built using **Python**, **scikit-learn**, and **Streamlit**nothing too fancy, just a straightforward ML project that works.

---

### 🧠 What It Does
- Takes an SMS or any text message as input  
- Cleans the text using **NLTK** (removes stopwords, punctuation, etc.)  
- Converts the text into numbers using **TF-IDF Vectorizer**  
- Uses a **Multinomial Naive Bayes** model to make predictions  
- Shows if the message is **Spam** or **Not Spam**

---
